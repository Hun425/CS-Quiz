package com.quizplatform.apigateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * API Gateway 기본 라우팅 설정
 * 보안 기능이 제거된 상태에서 기본 라우팅만 제공
 */
@Configuration
public class SimpleRoutingConfig {
    // 라우팅은 application.yml에서 처리하므로 여기는 비워둠
}
